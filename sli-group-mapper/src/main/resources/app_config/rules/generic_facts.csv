# Generic objects can be define via JSON strings.
#
# Every generic object must contain a name followed by a list of objects. The names must be labeled as "name", whereas
# the list of objects have no pre-determined keys. For example:
#
#   {"name":"nameValue"};
#   {"key1": value1}; {"key2": value2};
#

# List of User Groups
{ "name"  : "User_Groups"      };
  { "group" : "Default"        };
  { "group" : "Enterprise"     };
  { "group" : "Smartphone"     };
